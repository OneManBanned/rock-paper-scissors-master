interface Mode {
    options: string[];
    rules: string;
}

export let original: Mode = {
    options: ['rock', 'paper', 'scissors'],
    rules: '/assests/images/image-rules-bonus.svg',
}

export let bonus: Mode = {
    options: ['rock', 'paper', 'scissors', 'lizard', 'spock'],
    rules: '/assests/images/image-rules-bonus.svg',
}